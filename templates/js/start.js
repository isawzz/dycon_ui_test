window.onload = start; 

async function start() {

	let dMain = mBy('dMain');
	// mStyleX(dMain,{align:'center'});
	// //mCenterFlex(dMain);

	// let dForm = mBy('dForm');
	// mStyleX(dForm,{matop:20})

	let dStatus = mDiv(dMain,{align:'center',matop:25,fg:'red'},'dStatus','wirklich? so einfach geht das???');
}
